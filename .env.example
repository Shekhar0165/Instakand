# Environment Variables for Instakand

# Server
PORT=3000
NODE_ENV=development

# Redis (for Bull Queue)
REDIS_HOST=localhost
REDIS_PORT=6379

# ============ INSTAGRAM AUTHENTICATION ============
# Session cookie for authenticated scraping (HIGHLY RECOMMENDED)
# Without this, you can only get ~12-20 posts per hashtag
# How to get: Log in to Instagram in Chrome -> DevTools -> Application -> Cookies -> Copy 'sessionid' value
INSTAGRAM_SESSION_ID=

# Option 2: Auto-login (recommended - automatically refreshes session)
# Use a dedicated scraping account, NOT your personal Instagram!
INSTAGRAM_USERNAME=
INSTAGRAM_PASSWORD=

# ============ PROXY CONFIGURATION ============
# Proxy list (optional but recommended for high-volume scraping)
# Format: protocol://user:pass@host:port (comma-separated for multiple)
# Residential proxies work best!
# Examples:
# PROXY_LIST=http://user:pass@proxy1.com:8080
# PROXY_LIST=http://user:pass@proxy1.com:8080,http://user:pass@proxy2.com:8080
PROXY_LIST=

# Scraper Settings
MAX_CONCURRENT_BROWSERS=3
REQUESTS_PER_PROXY=50
MIN_DELAY_MS=3000
MAX_DELAY_MS=8000

# Rate Limiting
RATE_LIMIT_REQUESTS_PER_MINUTE=7
RATE_LIMIT_REQUESTS_PER_HOUR=300
